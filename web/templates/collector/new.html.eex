<h2>New collector</h2>

<%= render "form.html", changeset: @changeset,
                        action: location_collector_path(@conn, :create, @location.id) %>

<%= link "Back", to: location_collector_path(@conn, :index, @location.id) %>
