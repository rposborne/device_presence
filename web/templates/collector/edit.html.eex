<h2>Edit collector</h2>

<%= render "form.html", changeset: @changeset,
                        action: collector_path(@conn, :update, @collector) %>

<%= link "Back", to: collector_path(@conn, :index) %>
