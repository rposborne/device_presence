<h2>Edit device</h2>

<%= render "form.html", users: @users, changeset: @changeset,
                        action: device_path(@conn, :update, @device) %>

<%= link "Back", to: device_path(@conn, :index) %>
